# add_executable関数を使用して実行可能ファイルを作成
add_executable(
    # 第一引数は生成する実行可能ファイルの名前
    # この場合、実行可能ファイルの名前は"Sample1"になる
    Sample1

    # 以下の引数はソースファイルのリスト
    # これらのソースファイルはコンパイルされ、
    # その結果生成されたオブジェクトファイルがリンクされて実行可能ファイルを作成する
    # この場合、"main.cpp"の1つのソースファイルを指定
    main.cpp
)
