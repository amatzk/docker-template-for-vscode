{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/myproject",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake"
        }
    ]
}
